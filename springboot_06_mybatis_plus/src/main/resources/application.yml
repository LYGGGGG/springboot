# 数据库连接
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ssm_db?serverTimezone=UTC
    username: root
    password: admin

# 设置MP，表前缀
mybatis-plus:
  global-config:
    db-config:
      table-prefix: tbl_
